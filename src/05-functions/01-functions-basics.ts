// By default in typeScript the parameter has the type any it can be array, number , strings and others...

function sayHi(name: string) {
  console.log(`Hello ${name.toUpperCase()}`);
}

// sayHi(0)





// -any
// -config
// -set with the type
